- name: choco upgrade all
  hosts: all
  tasks:
  - name: Upgrade installed packages
    win_chocolatey:
      name: all
      state: latest
    register: result

  - name: report
    debug: var=result
